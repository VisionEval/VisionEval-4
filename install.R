# To use this script, set the path below to the location of the VE4-install.R script in your source tree.
# To run it from the root of your development source tree, set VE_INSTALL to the directory
# you would like to host ve-lib, downloads, etc. (it will default to using .gitignored folders in your
# development tree, which is convenient but confusing about what is in or not in the repository).
source(ve.url <- "n:/Git-Repos/VisionEval-40-dev/sources/framework/VEStart/inst/download/VE4-install.R")
